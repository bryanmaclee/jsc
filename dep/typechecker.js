export function type_checker(exp, env) {
   // console.log(exp, env);
   // if (exp.type === "constant declaration") {
   //    if (exp.evaluated) {
   //       env.Types.set(exp.name, typeof exp.evaluated);
   //    }
   // }
   // console.log(env);
   return exp;
}
